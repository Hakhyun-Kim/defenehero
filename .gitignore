# 의존성
node_modules/

# 로그/캐시
npm-debug.log*
*.log
.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/

# 애널리틱스 비공개 키 설정
src/analytics.config.js

# dist/game.js 는 커밋합니다 (빌드 없이 index.html 더블클릭만으로 실행되도록)
# 그래서 로컬 src/analytics.config.js 는 키를 비워 둔다 — 채워두면 번들에 박혀 히스토리로 샌다.

# Capacitor/Pages 배포용 산출물 (CI가 매번 새로 만든다)
www/

