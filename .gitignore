# 의존성
node_modules/

# 로그/캐시
npm-debug.log*
*.log
.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/

# dist/game.js 는 커밋합니다 (빌드 없이 index.html 더블클릭만으로 실행되도록)
# GameAnalytics 키(src/analytics.config.js)도 함께 커밋한다 — 웹 SDK 는 키를 브라우저에서
# HMAC 서명에 쓰므로 어차피 배포본에 노출된다. fork 유입은 키를 숨겨서가 아니라
# analytics.js 의 실행 위치 게이트(COLLECT_HOSTS)가 막는다.

# 빌드 산출물 (Capacitor www/ — 로컬에서 만든다)
www/

